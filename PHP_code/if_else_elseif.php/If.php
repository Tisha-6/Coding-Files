<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Conditional_Statements</title>
</head>
<body>


    <?php

    

    // // Data types means check int, string 

    // if (998 > 100) {

    //     echo"Good afternoon";
    //     echo"<br><br>";
    // }


    // $t = 52;

    // if ($t < 60) {
        
    //     echo "Why?";
    //     echo "<br><br>";
    // }

    // Returns true if the value are equal

    $t = 100;
    
    if ($t == 100){

        echo "very funny = Hilarious";
        echo"<br><br>";
    }
    
    // Returns true if the value and data types are identical

    $t = 100;

    if ($t === 100)

    echo "very painful = Excruciating";

    // Returns true if the value are not equal 

    $t = 80;
    $u = 94;

    if ($u != $t){

        echo "very Rich ";
        echo "<br><br>";
    }

    // Returns true if the value are not equal 

    $u = 100;
    $f = 78;

    if ($u <> $f){
        echo "very ";
    }

    // Returns true if the value or data types are not identical 

    $v = 100;
    $s = 45;

    if ($v !== $s){

        echo "That's true ";
    }

    // Returns true if the first value is greater than the second value

    $v = 100;
    $t = 79;

    if ($t >= 100){

        echo "It's false";

    }

    // Returns true if the first value is less than the second value

    $t = 100;
    $i = 89;

    if ($i <= 100){

        echo "That's false";
    }

    
    
    ?>

    


</body>
</html>